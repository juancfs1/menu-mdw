# Example WildFly configuration.
SERVICES=("wildfly" "wildfly-app01")

TECHNOLOGIES=("Wildfly")

# One entry per WildFly installation.
WILDFLY_INSTANCES=("/opt/wildfly" "/opt/wildfly-app01")

# One mode per instance: standalone or domain.
WILDFLY_MODES=("standalone" "standalone")
